name: kyogen
description: 今日も元気 — Daily Check-in App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  firebase_messaging: ^15.1.3
  google_sign_in: ^6.2.1
  cloud_functions: ^5.1.3
  url_launcher: ^6.3.0
  flutter_local_notifications: ^17.2.2
  shared_preferences: ^2.3.2
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
